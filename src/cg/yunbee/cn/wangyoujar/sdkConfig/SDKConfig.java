package cg.yunbee.cn.wangyoujar.sdkConfig;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

/** add new SDK **/
public abstract class SDKConfig {
	
	//private final Map<String, PropertyInfo>  mProInfoMap = new HashMap<String, PropertyInfo>();
	
	/*public void addPropertyInfo(String propId, PropertyInfo aibeiProInfo) {
		mProInfoMap.put(propId, aibeiProInfo);
	}
	
	public void addHashMap(Map<String, PropertyInfo>  ProInfoMap) {
		mProInfoMap.putAll(ProInfoMap);
	}*/
	
	/*public void addList(List<PropertyInfo> list) {
		int count = list.size();
		if(count > 0) {
			for(PropertyInfo proInfo : list) {
				mProInfoMap.put(proInfo.PropertyId, proInfo);
			}
		}
	}
	
	public PropertyInfo getPropertyInfo(String propId) {
		return mProInfoMap.get(propId);
	}

	public void clear() {
		mProInfoMap.clear();
	}*/
}